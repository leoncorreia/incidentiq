# Optional. If unset in local dev, the app calls same-origin `/api` and Vite proxies to port 10000
# (avoids CORS when you open the dev server via a LAN URL).
# VITE_API_URL=http://127.0.0.1:10000
# Production:
# VITE_API_URL=https://your-backend.onrender.com
